PORT=4003

# PostgreSQL connection (used by Schema Builder, Report Builder, SQL Executor)
PGHOST=localhost
PGPORT=5432
PGDATABASE=sfas_db
PGUSER=sfas_user
PGPASSWORD=sfas_user

# SSL mode for PostgreSQL connections
DB_SSL=false

# Capture All base URL
CAPTURE_BASE=http://localhost:4003
