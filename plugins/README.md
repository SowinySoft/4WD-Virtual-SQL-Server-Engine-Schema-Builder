# Schema Builder — Plugins Directory

Extend Schema Builder with custom object types, engine dialects, or output formats.

## Coming Soon

- Custom type mappings
- Custom template generators
- Export to ORM models (Prisma, TypeORM, Sequelize)
- Export to migration frameworks
